# Configura estos secretos en GitHub (mismos del repo heavenlydreamslovera):
# Settings → Secrets and variables → Actions → New repository secret
#
#   SERVER_HOST     → IP del servidor VPS
#   SERVER_USER     → usuario SSH
#   SERVER_SSH_KEY  → clave privada SSH
#
# NUNCA pongas contraseñas ni IPs directamente en este archivo.
